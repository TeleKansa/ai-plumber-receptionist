# Decisions (append-only)

## D-001 — 2026-06-11 — Session-1 audit findings
Repo audit: single-tenant app, no Postgres/tenant DB anywhere in code or requirements; no recording; no testing tenant or test-caller path exists. Live service healthy (`/health` ok, fetched 2026-06-11). Commit a2f0585 (docs-only) confirmed in git, local main == origin/main; Railway deploy NOT verifiable from session (no Railway CLI/token; GitHub unreachable from sandbox). Owner action queued (A-001). Live-call risk of a2f0585: none (zero code changed).

## D-002 — 2026-06-11 — Owner direction
Owner (Vincent), in chat: Twilio number and go-live scripts deferred. Proceed P0 then P1. Plumber behavior must be preserved exactly through the split. Return for number purchase + script approvals only after split passes acceptance.

## D-003 — 2026-06-11 — Operating assumption: repo is source of truth
Charter references "tenant config in Postgres" and a "testing tenant"; neither exists in this codebase. Until owner says a DB/service exists elsewhere, repo + Railway env vars are treated as the complete system. The charter's weekly DB-drift check is a no-op until a DB exists. Config-as-code rule applied to code-embedded config (exported to /config/tenants/).

## D-004 — 2026-06-11 — P1 acceptance method (see D-006 for results)
No live test path exists and test-number spend is deferred (owner, D-002). Charter Verification Protocol explicitly permits "scripted media-stream test" as deploy evidence. P1 acceptance therefore = (a) golden equivalence: byte-identical instructions, session.update, greeting, TwiML, SMS body, audio transcoding frames vs pre-refactor recordings; (b) scripted end-to-end media-stream call replay; (c) zero plumber strings in core/. Refactor stays on branch `p1/core-vertical-split`; merge to main (= production deploy) only after owner reviews evidence. Post-merge live call recommended as final confirmation.

## D-005 — 2026-06-11 — Discovery: legacy-snapshot/ explains the charter's "Postgres" reference
Untracked directory `legacy-snapshot/` found at repo root: a parked working copy of the SAME GitHub repo, checked out on branch `phase-1a-stability-guardrails` — a much larger multi-tenant variant (70KB main.py, Postgres via psycopg, admin pages, demo mode, its own test suite, milestones up to 5). Its `main` == `origin/main` == c4798de, the direct parent of our a2f0585 — so GitHub main has always been the simple single-file app, and the multi-tenant work lives on an unmerged branch. This resolves the D-003 mystery: the charter's "tenant config in Postgres" describes the phase-1a branch, not what GitHub main deploys. Cannot determine WHICH branch Railway actually deploys from HTTP probes (public routes identical) — folded into A-001. Security: legacy-snapshot contains a real `.env` and a GitHub PAT in its git config; added `legacy-snapshot/` to .gitignore so it can never be committed. Its test suite + admin code are valuable reference material for P2+.

## D-006 — 2026-06-11 — P1 acceptance evidence: 11/11 regression tests pass
Method per D-004. Goldens recorded from pre-refactor code at commit fb0e720 via scripted media-stream harness (full call: webhook → WS start → caller audio → greeting → AI audio → function call → SMS → hangup scheduling → stop). Refactor committed on branch `p1/core-vertical-split` @ c1aa2fa: core/engine.py (industry-agnostic) + core/vertical.py + verticals/plumbing.json (prompt/tools/greeting extracted programmatically — byte-identical round-trip asserted) + 16-line tenant entrypoint main.py. Results: 11/11 pass — TwiML, OpenAI URL, session.update (instructions + tools byte-identical), caller-audio transcode frame, greeting payload, all OpenAI frames, AI-audio frame to Twilio, SMS lead body/recipients, end-of-call session state, helper functions, and zero vertical strings in core/ (grep-enforced test). Awaiting owner review (A-002). NOT merged; production untouched.
[2026-06-11 correction, see D-008: this work targeted the wrong baseline — production runs the phase-1a multi-tenant build, not main's single-file app. A-002 withdrawn; branch archived as methodology reference.]

## D-007 — 2026-06-11 — Owner rulings (Railway facts + five ambiguity rulings)
Owner, from Railway dashboard: Source repo TeleKansa/ai-plumber-receptionist; production service deploys branch `phase-1a-stability-guardrails`, auto-deploy ON; Postgres service Online with volume; active production deployment dated May 21; the June 11 docs push to main triggered nothing (Railway does not watch main).
**LIVE WIRE RULE (permanent): any push to `phase-1a-stability-guardrails` auto-deploys to the live phone service. No push to that branch ever, without (a) full charter live-line safety protocol AND (b) explicit owner approval per change. The branch is production itself.**
Rulings: (1) Postgres tenant config confirmed real; config-as-code applies in full once consolidation lands. (2) Test path approved in principle — separate Railway service + dedicated test number ~$1–2/mo; scripted media-stream simulation is the P0/P1 verification tool; the physical test number purchase is batched with the deferred shoreline Twilio funding (one Twilio errand); anything only a real call can verify gets queued and flagged. (3) Metrics: lightweight call-outcome logging approved as a P1 item; until then numbers pulled manually from Railway logs/Twilio console with source noted in METRICS.md. (4) Repo rename: leave as-is, low-priority hygiene. (5) Owner revoking the legacy embedded PAT; the claude-fleet token is the only valid credential. Python 3.12/audioop pin noted for hardening list.

## D-008 — 2026-06-11 — Reconciliation of production branch and legacy-snapshot (read-only)
**Production branch confirmed.** `origin/phase-1a-stability-guardrails` @ 77b5537 ("Fix startup migration diagnostics"), last commit 2026-05-21 21:32:41 -0500 — consistent with the May 21 production deployment (byte-level deploy SHA can't be confirmed externally; no /version endpoint yet). Contents verified multi-tenant: SQLAlchemy 2.0.36 + psycopg3; storage/ (database, migrations, models, repository) with Tenant + TenantAIProfile (the charter's "tenant prompt profiles in Postgres") and tenant_id-keyed Call/Lead/Notification/CallEvent; per-number routing via resolve_tenant_phone_for_number(To); telephony gate with tenant statuses live/testing/paused/onboarding INCLUDING the allowed_test_callers test path — the charter's testing tenant exists in the production build; workflow/prompt_builder.py + intake/notification policy modules; admin UI; its own test suite. Divergence: merge-base c4798de; phase-1a +24 commits; main +1 (a2f0585) + 2 local ops commits (unpushed).
**legacy-snapshot contains ZERO unique work.** Its HEAD == 77b5537 == fresh origin tip (nothing unpushed); working tree clean; no stash; no untracked source files; other local branches (main @ c4798de, chore @ d322ba2) hold nothing unpushed. Only ignored artifacts: .env (live secrets — owner to archive values, file never committed), .venv/, __pycache__/. Folder was gitignored in session 2 on both local branches. Safe to delete after owner secures the .env.
**Correction to session 2:** the P1 core/vertical split (c1aa2fa) refactored main's single-file app, which is not what production runs. A-002 withdrawn. Salvage: the golden-recording + scripted media-stream harness methodology and the vertical-config design transfer directly to the real §1.1 refactor, which must target workflow/prompt_builder.py after consolidation.
**Consolidation plan proposed as A-003 / ops/CONSOLIDATION_PLAN.md — no consolidation action taken pending owner decision.**

## D-009 — 2026-06-11 — A-003 APPROVED; Phase B/C arrangements fixed
Owner approved CONSOLIDATION_PLAN as written. Phase A executes now; completion criteria exactly as planned (byte-identity diff vs production tip + branch test suite green in sandbox), evidence to DECISIONS, then STOP and wait. Phase B window: a weekday evening this week after 21:00 CDT — specific day to be chosen from call-pattern data in the Phase A report; owner personally clicks the Railway Source change; operator directs in real time and owns post-cutover green-light confirmation + verification call + transcript archiving. Phase C as planned: phase-1a frozen 2 weeks as rollback insurance; /version endpoint and call-metrics logging are the first two changes under the new protocol; only then the real prompt_builder refactor. Owner also credited the D-008 self-invalidation — honest corrections are expected behavior.

## D-010 — 2026-06-11 — Phase A COMPLETE; evidence; STOPPED awaiting owner
- A1: tags pushed — `archive/single-file-app` @ a2f0585, `archive/p1-split-of-single-file-app` @ c1aa2fa.
- A2: ops-only main pushed, origin/main a2f0585 → b8bf63b (4 ops commits; zero code).
- A3: branch `consolidation` @ 85d5294 = merge of main into production tip 77b5537. Only conflict: .gitignore → resolved by union (keeps .env + legacy-snapshot/ exclusions). Merge performed in a /tmp clone to avoid the mount's deletion restrictions, then pushed back.
- A4 GATE PASSED, evidence:
  (1) Byte-identity: `git diff 77b5537..consolidation -- '*.py' main.py requirements.txt Procfile railway.json .python-version .env.example README.md AGENTS.md admin/ storage/ workflow/ scripts/ tests/ docs/ config/settings.py` → **0 bytes**. Full delta = 14 files, +399 lines: 13 docs/ops files added + .gitignore union. The cutover will deploy byte-identical code.
  (2) Branch's own test suite in sandbox: **137 passed, 0 failed, 0 skipped** (tests run against their own fixtures; no live Postgres required).
  (3) Boot check: app imports with dummy env; 33 routes (/voice, /media-stream, /health + 26 admin routes).
  (4) Env-var audit from config/settings.py (15 names): OPENAI_API_KEY, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER, PLUMBER_PHONE_NUMBER, DATABASE_URL, ADMIN_PASSWORD, HOST, PUBLIC_HOST, OAI_URL, OPENAI_REALTIME_MODEL, OPENAI_REALTIME_URL, DEFAULT_TENANT_GREETING, DEFAULT_TENANT_NAME, DEFAULT_TENANT_SLUG. All service-level in Railway — unaffected by a Source-branch change.
- Push verification (ls-remote): consolidation 85d5294, main b8bf63b, **phase-1a-stability-guardrails UNCHANGED at 77b5537**. No push ever targeted the production branch.
- STOPPED per D-009. Pending owner: (a) merge consolidation→main approval (A-003 step A5) — can be batched with the cutover-day decision; (b) cutover-day choice needs call-pattern data — operator has NO Railway log access (no token); owner must either read the week's /voice log timestamps from Railway Observability himself or provide a read-scoped project token (request logged in APPROVAL_QUEUE).

## D-011 — 2026-06-12 — CUTOVER EXECUTED (Phase B); verification call archived; one open question
Owner rulings in-session: call-pattern analysis cancelled (zero real traffic); cutover immediately; A5 approved. Sequence executed: consolidation merged → GitHub main @ ac687be (code diff vs production tip 77b5537 = 0 bytes, re-verified post-merge; boot check OK); ls-remote confirmed phase-1a untouched at 77b5537 before the flip. Owner repointed Railway Source phase-1a→main 04:43–04:45 UTC; operator probed /health + /media-stream externally 04:45:23–04:47Z — service up throughout, no downtime observed.
Verification call (owner-placed, 04:53 UTC, CA9ef88ded…): answered, multi-tenant pipeline confirmed live — tenant_id=1 routing, model gpt-realtime-2 (NOTE: not the 1.5 the old single-file app pinned; model comes from tenant/env config — INFRASTRUCTURE.md corrected), full intake captured incl. extra_fields (property_role, additional_notes). Full annotated log: ops/transcripts/2026-06-12_cutover_verification_CA9ef88ded.md.
RESOLVED 2026-06-12: owner confirmed the lead SMS arrived within seconds ("线索短信秒到") — delivery worked end to end. The snapshot's lead_id=None is therefore a logging/snapshot quirk, not a delivery failure; downgraded to a low-priority curiosity to examine once DB read access exists (BACKLOG). **PHASE B CLOSED: cutover verified end-to-end — answered, qualified, lead delivered.**
LIVE WIRE TRANSFERRED: main is now production. No push to main without protocol + per-change owner approval. Operational consequence: even docs-only ops commits trigger a redeploy if pushed — mitigation proposed to owner: Railway "watch paths" setting to exclude ops/** and *.md from triggering builds. Until decided, ops commits stay local-only.

## D-012 — 2026-06-12 — Phase C change #1 ready: /version endpoint (awaiting A-004)
Branch `change/version-endpoint` @ 1b54a42 (pushed; deploy-inert). Diff: +9 lines main.py (adds `import os` + GET /version returning Railway-injected RAILWAY_GIT_COMMIT_SHA/RAILWAY_GIT_BRANCH, "unknown" fallback; no new dependencies, no startup/DB involvement) + new tests/test_version.py. Scripted verification: full suite 139 passed (137 pre-existing + 2 new), 0 failed. Deploy plan on A-004 approval: merge to main + single push (also carries local ops commits) → Railway redeploys → verify /version returns the pushed SHA from outside — first externally self-verifiable deploy. Change #2 (call-metrics read path) to be scoped next session against storage/repository.py. After both: core/vertical refactor of workflow/prompt_builder.py; shoreline vertical = first client (owner directive 2026-06-12).

## D-013 — 2026-06-12 — Phase C change #1 DEPLOYED & verified: /version endpoint live (A-004 executed)
Owner approved A-004 and set Railway **Watch Paths** (operator-provided value, verified against Railway build-configuration docs) so docs no longer trigger deploys:
```
**
!/ops/**
!/**/*.md
```
Order per owner: watch paths set FIRST, then push — so this and all future docs-only changes don't disturb the live line.
- **Pre-push verification** (isolated /tmp clone; deploy-inert): merged origin/change/version-endpoint → main = `74e5dbe`. Code delta vs deployed tip ac687be = `main.py` +9 (the /version route: returns RAILWAY_GIT_COMMIT_SHA/RAILWAY_GIT_BRANCH with "unknown" fallback; no new deps, no DB, no startup hook) + `tests/test_version.py` only — rest is docs. Full suite **139 passed / 0 failed** (Python 3.10, dummy env, SQLite fallback); boot check 39 routes incl. /version, /voice, /media-stream. One transient suite failure (test_tenant_pages_render) was isolated to a harness artifact (operator's DEFAULT_TENANT_NAME=Test env override) — reproduced IDENTICALLY on deployed tip ac687be, proving /version was not the cause; re-ran with the var unset → 139/139.
- **Push**: `ac687be..74e5dbe` → origin/main (exit 0). `phase-1a-stability-guardrails` re-verified UNCHANGED at 77b5537 before and after.
- **Deploy**: Railway auto-built main; owner confirmed top deployment ACTIVE + "Deployment successful", titled "Phase C #1: merge /version endpoint into main (A-004; D-012)".
- **External self-verification**: `GET /version` → `{"commit":"74e5dbe7255e37da3548cc09a604bcd77b64edc4","branch":"main"}` — exact match to pushed SHA. First externally self-verifiable deploy. `/health` returned `{"status":"ok"}` on every probe across the instance swap — no observed downtime. (Verification artifact: a per-URL response cache served a stale empty 404 for /version until a cache-busting query param was used — not a deploy issue.)
- **A-004 CLOSED.** Deploy evidence complete per Verification Protocol.
LIVE WIRE unchanged: main is production; no code push without protocol + per-change owner approval. Docs-only pushes now deploy-inert via Watch Paths (the ops commit carrying this entry is the first such push; ACTIVE deploy SHA must remain 74e5dbe — confirmed in session).

## D-014 — 2026-06-12 — Phase C change #2 BUILT: /admin/metrics.json read path (awaiting A-005)
Owner chose minimal read-only JSON scope (Option A). Branch `change/metrics-json` @ 4e2ccba — pushed; deploy-inert (Railway watches main only; main 368df2c + phase-1a 77b5537 re-verified unchanged after the branch push).
- Change: new auth-protected `GET /admin/metrics.json` in admin/routes.py (+46/-1) reusing the already-tested `repository.pilot_metrics` and adding charter-derived rates: `calls_answered`, `qualification_completion_rate`, `sms_delivery_rate`, `calls_without_leads`, emergency/unreviewed/bad-or-needs-review counts, and `by_tenant` rows each with their own qualification rate + `generated_at`. No schema change, no call-path involvement, no new deps. Router-level admin auth already applies (401 without creds).
- Tests: tests/test_metrics_json.py (+128) — auth required (401), derived-rate correctness (0.75 qualification, 0.6667 SMS delivery on a 4-call/3-lead/1-fail fixture), zero-denominator → null. **Full suite 142 passed / 0 failed** (139 prior + 3 new). Sample payload verified via TestClient (200 with auth, 401 without).
- NOT merged to main; production untouched. Deploy plan on A-005: merge change/metrics-json → main + single push → Railway redeploys → verify GET /version returns the new SHA and GET /admin/metrics.json returns 200 (admin-auth) from outside.
Next after #2 deploys: core/vertical refactor of workflow/prompt_builder.py (shoreline = first client). Twilio number + consent/greeting scripts remain deferred (owner).

## D-015 — 2026-06-12 — Phase C change #2 DEPLOYED: /admin/metrics.json live (A-005 executed) + PRIORITY PIVOT
- Merge change/metrics-json → main = 7a9c3df; code delta vs prior main 6110f6b = admin/routes.py (+46/-1) + tests/test_metrics_json.py only. Full suite **142 passed / 0 failed** on the merged tree.
- Push 6110f6b..7a9c3df → origin/main (exit 0). phase-1a re-verified UNCHANGED at 77b5537 before/after.
- External verification: `GET /version` → `{"commit":"7a9c3df…","branch":"main"}` (exact merge SHA → deploy live). /admin/metrics.json ships in that commit; it is admin-auth gated, so an unauthenticated external probe returns 401 (not 200) — owner can confirm the JSON in-browser with the admin password. No call-path change; read-only.
- **A-005 CLOSED.** Phase C #1 (/version) + #2 (metrics.json) both deployed & verified.
- **PRIORITY PIVOT (owner directive 2026-06-12):** the prompt_builder core/vertical refactor (Phase C #3) now aims STRAIGHT at a usable shoreline vertical — Loopline's first business-value output. Refactor scope = only what shoreline needs; plumbing preserved exactly. All nice-to-have internal work (more metrics, more self-verification endpoints, weekly-report automation) is deprioritized behind the shoreline critical path. Every session report opens with "steps to shoreline's first real homeowner call." Canonical 6-step path recorded in STATE.md.

## D-016 — 2026-06-12 — Shoreline scripts drafted for approval (A-006), unblocking part of critical-path step 4
Per owner ("先起草"), drafted greeting / identity / consent for shoreline → `config/tenants/shoreline_scripts_DRAFT.md`. Wording sourced verbatim from Integration Contract §1.2 (greeting_line, identity_rules, consent_script) + §1.3 (recording disclosure) — no silent divergence from the contract. Surfaced the record/don't-record decision: FL is two-party-consent, so the recommended Option-B greeting carries a recording-disclosure line; recording stays OFF until explicit owner yes. Flagged TCPA/lead-sharing consent + FL recording as legal-adjacent and recommended counsel review. NOT live; awaiting A-006. Remaining step-4 item after script approval = buying the shoreline Twilio number (owner errand). No code/call-path change; deploy-inert (.md).

## D-017 — 2026-06-12 — Phase C #3 (shoreline step 1) STARTED: golden regression baseline locked
Began the prompt_builder core/vertical refactor the safe way — established the byte-identity safety net BEFORE touching live-call code. Branch `change/core-vertical-split` @ 2040765 (pushed; deploy-inert).
- Mapped the live assembly: main.build_session_update → make_instructions → PromptBuilder.build (workflow/prompt_builder.py) + TOOLS (submit_service_request) + realtime_session_overrides; greeting via build_initial_greeting_response. Plumbing-specificity lives in prompt_builder.py (5 fields issue/urgency/address/callback/name; leak/clog/water-heater/toilet triage; shutoff) and TOOLS — these must move into a plumbing vertical config; core must end up industry-agnostic.
- Added tests/test_prompt_session_golden.py + tests/goldens/prompt_session_baseline.json: captures the EXACT current session.update (instructions + tools + overrides) + greeting for 3 plumber configs (default; property_role + additional_notes intake mirroring prod tenant_id=1; custom style profile). **Full suite 143 passed.** The refactor must keep this byte-identical.
- Next block: design core-engine API + vertical-config schema (minimal for shoreline) → implement split (plumbing → verticals config, zero plumber strings in core) → assert byte-identity vs this golden + a leakage test. On-branch, deploy-inert; deploy only after owner approval + a plumber-line regression test call (only-a-real-call-can-verify queue).
